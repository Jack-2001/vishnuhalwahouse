# Frontend — Vishnu Halwa House

Quick start:

```bash
cd frontend
npm install
npm start
```

The app serves the logo at `/logo.svg` (copied to `public/`) and proxies API requests to the backend at `http://localhost:5000` using the `proxy` setting.
